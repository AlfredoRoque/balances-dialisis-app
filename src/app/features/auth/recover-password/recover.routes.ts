import { Routes } from "@angular/router";
import { RecoverPasswordComponent } from "./recover-password.component";

export const RECOVER_ROUTES: Routes = [
  { path: '', component: RecoverPasswordComponent }
];
